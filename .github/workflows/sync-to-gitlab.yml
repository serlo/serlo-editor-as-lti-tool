name: Sync to GitLab
on:
  push:
    branches:
      - development
      - staging
      - production

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync to GitLab
        uses: kulla/sync-to-gitlab@v1
        with:
          owner: stephan_kulla
          repository_name: serlo-editor-as-lti-tool
          token: ${{ secrets.GITLAB_TOKEN }}
